var CronJob = require("cron").CronJob;
var async = require('async');
var moment = require("moment");
var dc = require('mcp_db').dc;

var config = require('mcp_config');
var prop = config.prop;
var game = config.game;

var esut = require("easy_util");
var log = esut.log;
var digestUtil = esut.digestUtil;

var service = require("mcp_service");
var termSer = service.termSer;
var msgSer = service.msgSer;
var notifySer = service.notifySer;

var cons = require('mcp_constants');
var termStatus = cons.termStatus;
var msgStatus = cons.msgStatus;
var msgType = cons.msgType;
var gameType = cons.gameType;

var MasterTermHandle = function(){};

MasterTermHandle.prototype.handle = function(msg, dTerm, cb)
{
    var self = this;
    async.waterfall([
        //处理消息
        function(cb)
        {
            if(dTerm.status == termStatus.PRE_ON_SALE)
            {
                self.open(msg, dTerm, cb);
            }
            else if(dTerm.status == termStatus.PREEND)
            {
                self.close(msg, dTerm, cb);
            }
            else if(dTerm.status == termStatus.PRE_SEND)
            {
                self.backClose(msg, dTerm, cb);
            }
            else if(dTerm.status == termStatus.IN_PAYOUT)
            {
                self.prize(msg, dTerm, cb);
            }
            else if(dTerm.status == termStatus.IN_SEAL)
            {
                self.seal(msg, dTerm, cb);
            }
            else
            {
                cb(null, null);
            }
        },
        //标记消息已经处理完成
        function(data, cb)
        {
            msgSer.updateStatus(msg._id, msgStatus.FINISHED, cb);
        }
    ], function (err, result) {
        //如果有父消息，则处理父消息
        if(msg.parentId)
        {
            msgSer.childMsgFinished(msg, dTerm, function(err, msgAfter){
                if(err)
                {
                    cb(err, msgAfter);
                    return;
                }
                if(msgAfter && msgAfter.finishCount == msgAfter.subCount - 1)
                {
                    self.handleParent(msgAfter, dTerm, cb)
                }
                else
                {
                    cb(err, msgAfter);
                }
            });
        }
        else
        {
            cb(err, result);
        }
    });
}

/**
 * 父消息处理完成
 * @param msg
 * @param detail_term
 * @param cb
 */
MasterTermHandle.prototype.handleParent = function(msg, dTerm, cb)
{
    var self = this;
    async.waterfall([
        //处理消息
        function(cb)
        {
            if(dTerm.status == termStatus.IN_CALCULATE)
            {
                self.check(msg, dTerm, function(err, data){
                    cb(err);
                });
            }
            else
            {
                cb(null);
            }
        },
        //标记消息已经处理完成
        function(cb)
        {
            msgSer.updateStatus(msg._id, msgStatus.FINISHED, cb);
        }
    ], function (err, result) {
        cb(err, result);
    });
}

/**
 * 期次开售
 * @param msg
 * @param detail_term
 * @param cb
 */
MasterTermHandle.prototype.open = function(msg, dTerm, cb)
{
    var self = this;
    var cond = {id:dTerm.termId, status:termStatus.ON_SALE};
    async.waterfall([
        //更新期次状态
        function(cb)
        {
            termSer.updateStatusById(cond, cb);
        },
        //生成期次通知
        function(term, cb)
        {
            notifySer.saveTerm(term, cb);
        }
    ], function (err, result) {
        cb(err, result);
    });
}

/**
 * 期次停售
 * @param msg
 * @param detail_term
 * @param cb
 */
MasterTermHandle.prototype.close = function(msg, dTerm, cb)
{
    var cond = {id:dTerm.termId, status:termStatus.END};
    async.waterfall([
        //更新期次状态
        function(cb)
        {
            termSer.updateStatusById(cond, cb);
        },
        //生成期次通知
        function(term, cb)
        {
            notifySer.saveTerm(term, cb);
        }
    ], function (err, result) {
        cb(err, result);
    });
}


/**
 * 期次后台停售
 * @param msg
 * @param detail_term
 * @param cb
 */
MasterTermHandle.prototype.backClose = function(msg, dTerm, cb)
{
    log.info(dTerm);
    var cond = {id:dTerm.termId, status:termStatus.WAITING_DRAW_NUMBER};
    async.waterfall([
        //更新期次状态
        function(cb)
        {
            termSer.updateStatusById(cond, function(err, data){
                cb(err);
            });
        },
        //开奖(没有传入开奖号码，如果库中数据本身也没有开奖号码，则不会进行开奖操作)
        function(cb)
        {
            var dTermGame = game.getInfo(dTerm.gameCode);
            if(dTermGame.type == gameType.Gaopin)   //如果是高频游戏，校验是否可以开奖
            {
                var fromTerm = {id:dTerm.termId};
                termSer.draw(fromTerm, cb);
            }
            else
            {
                cb(null, null);
            }
        },
        //发送期次已经开奖的消息(高频，且期次已经开奖)
        function (term, cb) {
            var dTermGame = game.getInfo(dTerm.gameCode);
            if(dTermGame.type == gameType.Gaopin && term && term.status == termStatus.DRAW)
            {
                notifySer.saveTerm(term, cb);
            }
            else
            {
                cb(null);
            }
        }
    ], function (err, result) {
        cb(err, result);
    });
}

/**
 * 期次算奖结束，生成新的消息，用来生成文件
 * @param msg
 * @param detail_term
 * @param cb
 */
MasterTermHandle.prototype.check = function(msg, dTerm, cb)
{
    var self = this;
    log.info("check---------------------------");
    log.info(dTerm);
    msgSer.saveTermFileMsg(dTerm, function(err, data){
        cb(err, null);
    });
    /*
    var cond = {id:dTerm.termId, status:termStatus.CALCULATE};
    async.waterfall([
        //更新期次状态
        function(cb)
        {
            termSer.updateStatusById(cond, cb);
        },
        //生成期次通知
        function(term, cb)
        {
            notifySer.saveTerm(term, cb);
        }
    ], function (err, result) {
        cb(err, result);
    });*/
}

/**
 * 期次返奖
 * @param msg
 * @param detail_term
 * @param cb
 */
MasterTermHandle.prototype.prize = function(msg, dTerm, cb)
{
    var cond = {id:dTerm.termId, status:termStatus.PAYOUT};
    async.waterfall([
        //更新期次状态
        function(cb)
        {
            termSer.updateStatusById(cond, cb);
        },
        /*//生成期次通知
        function(term, cb)
        {
            notifySer.saveTerm(term, cb);
        }*/
    ], function (err, result) {
        cb(err, result);
    });
}

/**
 * 期次返奖
 * @param msg
 * @param detail_term
 * @param cb
 */
MasterTermHandle.prototype.seal = function(msg, dTerm, cb)
{
    var cond = {id:dTerm.termId, status:termStatus.SEAL};
    async.waterfall([
        //更新期次状态
        function(cb)
        {
            termSer.updateStatusById(cond, cb);
        },
        //生成期次通知
         function(term, cb)
         {
         notifySer.saveTerm(term, cb);
         }
    ], function (err, result) {
        cb(err, result);
    });
}

module.exports = new MasterTermHandle();