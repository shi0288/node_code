var TermReportType = function(){
    var self = this;
    self.info = [{id:1000, code:'SALE', des:'销售'},
        {id:1100, code:'PRINT', des:'出票'}];
    self.infoArray = {};
    self.init();
};

TermReportType.prototype.init = function()
{
    var self = this;
    for(var key in self.info)
    {
        var set = self.info[key];
        self.infoArray[set.id] = set;
        self[set.code] = set.id;
    }
};

TermReportType.prototype.getInfoById = function(id)
{
    var self = this;
    var obj = self.info;
    if(id != undefined)
    {
        obj = self.infoArray;
        obj = obj[id];
    }
    return obj;
};

module.exports = new TermReportType();

